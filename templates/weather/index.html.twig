{% extends 'base.html.twig' %} {% block title %}WeatherApp Startegia{% endblock %} {% block body %}
<div class="index">
    <h2 class="label">Saisissez une ville et valider</h2>
    {{ form_start(form) }}
    <div class="form">
        {{ form_widget(form.nom) }}
        {{ form_errors(form.nom) }}
        <button type="submit">Valider</button>
    </div>
    {{ form_end(form) }}
</div>

{% endblock %}